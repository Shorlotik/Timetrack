spring:
  datasource:
    url: jdbc:postgresql://db:5432/timetrack
    username: postgres
    password: Shorlotik003
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.xml
